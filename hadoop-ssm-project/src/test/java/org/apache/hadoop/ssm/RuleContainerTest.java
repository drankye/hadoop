package org.apache.hadoop.ssm;

import org.apache.hadoop.ssm.api.Expression.*;
import org.junit.Test;

/**
 * Created by root on 11/10/16.
 */
public class RuleContainerTest {
  @Test
  public void test1() {
    /*FileFilterRule fileFilterRule = new FileFilterRule("");
    TreeNode root = new TreeNode(, null, null);
    Action action = Action.ARCHIVE;
    SSMRule ruleObject = new SSMRule(fileFilterRule, root, action);
    long updateDuration
    RuleContainer ruleContainer = new RuleContainer(ruleObject, updateDuration, dfsClient);*/
  }
}
